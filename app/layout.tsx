import type { Metadata } from "next";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

export const metadata: Metadata = {
  title: "Breaking Gravity — Empowerment Coaching with Nicoll Mishel",
  description:
    "Turn fear into power. Breaking Gravity pairs empowerment coaching with experiential learning — including skydiving — to help women break through barriers and create lasting change.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>
        <Navbar />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
